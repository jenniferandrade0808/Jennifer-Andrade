# Marginal Analysis

Reading price against marginal cost off a table, a chart, and an optimizer's answer to decide how much of a constrained resource to allocate.

Exercised in: the Perfect Competition case (`docs/briefs/perfect-competition-brief.md`) — `spec.md` is the committed specification, `model.xlsx` is the workbook built from it.
